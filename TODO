1. If no command is given, do the query command

2. For query of settings add command line options (for both bios and for idrac) to:

    - print out all available settings
    - query a specific setting(s) by name
    - set a specific setting to the provided new value

    The default should remain as displaying the curated list of settings

    if bios or drac is not specified, `dracli settings 10.46.96.160` should report both bios and drac settings.

3. change "LARGE WARNING" to "WARNING"

4. query is simply too slow.  Let's either make it incremental (so the system is displayed first and futher details as they arrive) or make separate command for query versus inventory.  Let's also add the "status" output to the query command.

5. Let's add the dell service tag to the query output: "Serial Number: ABC1234D"

6. let's see if we can find the idrac hardware version like idrac9 or whatever, including this in the query output.

7. The query output is confusing because mem and cpu are system attributes, not idrac attriutes.  So change the order as follows:

    System: Dell Inc. PowerEdge XE8640
    Serial Number: ABC1234D
    Memory: 2048 GiB
    CPU: 2 x Intel(R) Xeon(R) Platinum 8468 (96 cores, 192 threads)
    BIOS: 2.8.2
    iDRAC: 16G Monolithic 7.30.10.50
